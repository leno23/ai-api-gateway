## Cursor Cloud specific instructions

This repository is currently empty (blank `README.md` only). There are no services, dependencies, build tools, or tests to run.

When application code is added, update this section with:
- How to install dependencies
- How to run, build, lint, and test the application
- Any non-obvious caveats for the development environment
