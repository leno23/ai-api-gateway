# Backend（AI API Gateway）

Go 网关模块根目录。在此目录执行所有 `go` 命令（`go build`、`go test`、`go run`）。

- **入口**：`cmd/server`
- **业务与 OpenAPI**：`internal/`
- **SQL 迁移**：`migrations/`
- **环境变量模板**：`.env.example`

仓库总览与本地依赖（Docker）见上级 [`README.md`](../README.md)。
