interface:
  display_name: "Ai Api Gateway"
  short_description: "Repo-specific patterns and workflows for ai-api-gateway"
  default_prompt: "Use the ai-api-gateway repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true